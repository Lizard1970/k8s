apiVersion: v1
kind: Service
metadata:
  name: demo-service
  labels:
    app: demo-service
spec:
  type: NodePort
  selector:
    app: demo-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
